{
  "name": "react-1",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:node": "node src/node-server/index.js",
    "start:nest": "node src/nest-server/build/main.js",
    "prepare:node-server": "npm install",
    "prepare:nest-server": "cd src/nest-server && npm install",
    "build:nest-server": "cd src/nest-server && npm run build",
    "prepare:client": "cd src/client && npm install",
    "build:client": "cd src/client && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
